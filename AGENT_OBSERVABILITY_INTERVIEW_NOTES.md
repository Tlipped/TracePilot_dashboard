# TracePilot 工程化可观测性与报告导出说明

## 这几轮前端工程化做了什么

这次升级的目标不是继续堆 Markdown，而是把多 Agent 分析过程拆成可扫读、可复盘、可导出的工作台视图。

1. **Agent Brief**
   - 统计每个 Agent 的事件数、工具调用数、结果输出数、Markdown 输出数、错误/告警数。
   - 展示每个 Agent 的最新输出摘要。
   - 点击 Agent 卡片可以联动过滤日志流和时间线。

2. **Agent Timeline**
   - 从 WebSocket/历史恢复事件中提取关键节点。
   - 保留任务状态变化、工具调用、结果输出、错误和告警。
   - 过滤掉大部分低价值 debug 噪声，让用户先看流程，再点进完整日志。

3. **Structured Final Report**
   - 将最终 Markdown 报告拆成五个固定区块：漏洞根因、攻击路径、关键交易、补丁建议、验证结果。
   - 如果报告本身有对应标题，就按标题抽取；如果没有，就基于关键词和交易哈希做 fallback 提取。
   - 原始报告仍然保留在 Raw Report 区块，避免结构化展示损失上下文。

4. **Audit Export**
   - 支持导出 Markdown 审计报告，便于人读和投递。
   - 支持导出 JSON evidence package，包含任务元数据、结构化报告区块、Agent 摘要和当前事件流。
   - 这让平台从“能看日志”进一步变成“能复现、能交付、能审计”。

可以这样总结：**实时流负责“看现在”，文件日志负责“可复现”，Brief/Timeline 负责“看得懂”，结构化报告和导出负责“可交付”。**

## 技术实现细节

前端没有新增复杂状态管理库，而是基于已有事件流派生视图：

- `Dashboard.tsx` 维护统一的 `events: TaskEvent[]`。
- `LogStream` 直接渲染原始事件。
- `AgentTimeline` 从 `events` 中筛选关键事件，按时间顺序展示。
- `AgentInsights` 对 `events` 做聚合统计，生成每个 Agent 的摘要卡。
- `StructuredReport` 对 `task.final_report` 做章节解析，并复用 `events` 生成导出证据包。
- `LogDetailDrawer` 仍然负责展示完整 Markdown/JSON，避免主页面被大段文本淹没。

导出能力完全在前端完成：使用 `Blob` + `URL.createObjectURL` 生成 Markdown 或 JSON 文件，不依赖额外后端接口。这样适合当前科研原型向工程平台过渡的阶段，后续如果要做团队共享、权限控制或长期归档，再把导出能力沉到后端。

## 结构化报告怎么解析

当前实现采用轻量规则解析：

- 先扫描 Markdown 标题，如 `漏洞根因`、`Root Cause`、`Patch Suggestions` 等。
- 如果标题命中，就提取该标题下的正文作为对应区块。
- 如果标题不命中，就按关键词从段落里找 fallback。
- `关键交易` 区块额外从报告和最近日志中提取 `0x...` 交易哈希。
- 最后保留 Raw Report，保证原始上下文不丢。

这不是用向量库或 LLM 再总结，而是一个确定性展示层。好处是可解释、成本低、不会引入二次幻觉。

## 面试可能被问到的问题

### 1. 为什么要做 Agent 可观测性？

可以回答：

多 Agent 系统的问题不是只看最终答案，而是要知道每个 Agent 在什么时候基于什么证据做了什么判断。TracePilot 面向漏洞定位，任何错误结论都可能影响补丁生成和验证，所以我把实时日志、Agent 状态、时间线和文件日志结合起来，支撑调试、复盘和可复现。

### 2. 为什么不直接展示大模型 Markdown？

可以回答：

大模型输出通常很长，且混合了思考、工具调用、阶段结论和 JSON 结果。如果全部平铺展示，用户很难快速判断系统卡在哪里。因此我把输出分成两层：主界面展示结构化摘要和 Timeline，完整 Markdown 放在 Drawer、Raw Report 或文件日志里按需查看。

### 3. WebSocket 日志断连后怎么办？

可以回答：

实时阶段通过 WebSocket 推送事件；任务完成或断连后，可以从数据库和本地 Agent 日志文件恢复。前端刷新时先拉任务状态，再重连 WebSocket，同时 File Logs 提供离线复盘入口。这保证了分析过程不是一次性流式输出，而是可以恢复和审计的执行记录。

### 4. Redis 和数据库/文件日志怎么分工？

可以回答：

Redis 更适合存短期高频日志内容和大段完整消息，支撑实时查看和缓存；数据库存任务状态、日志元数据和可查询记录；文件日志保留 Agent 原始运行轨迹，便于离线排查。三者分别服务实时性、结构化查询和长期复盘。

### 5. 多 Agent 如何避免结论漂移？

可以回答：

一方面通过角色边界拆分 Agent，比如交易筛选、角色识别、Trace Debug、补丁生成和验证；另一方面通过共享任务状态、日志持久化和验证反馈闭环，让后续 Agent 能看到前序证据，而不是只凭单次模型输出。前端可观测性则帮助发现哪一步开始偏移。

### 6. 为什么报告结构化不用 LLM 再总结？

可以回答：

当前最终报告本身已经是模型链路的产物，如果再调用一次 LLM 总结，可能引入二次幻觉，也会增加成本和延迟。因此这里采用确定性规则：标题优先、关键词 fallback、交易哈希正则抽取。这样可解释、可复现，也符合审计场景对证据稳定性的要求。

### 7. JSON evidence package 有什么价值？

可以回答：

Markdown 报告适合人读，JSON 证据包适合机器复现和后续分析。它包含任务元数据、结构化报告区块、Agent 摘要和事件流，可以用于离线排查、自动评测、回归对比，或者后续接入知识库和案例库。

### 8. 这套能力迁移到华为终端云有什么价值？

可以回答：

我做的不是只针对区块链的页面，而是一套长链 Agent 任务的可观测模式：任务生命周期管理、实时事件推送、状态恢复、结构化日志摘要、离线复盘和报告导出。迁移到终端云场景，可以用于智能客服、日志诊断、应用运维、安全分析等长流程 AI 系统。

## 可以在简历/面试里这样讲

我在 TracePilot 的工程化阶段补齐了多 Agent 分析过程的可观测和可交付能力。系统通过 WebSocket 推送任务状态和 Agent 中间结果，前端将原始日志进一步组织为 Agent Brief、Timeline、实时日志流、文件日志和结构化最终报告。任务完成后，用户可以导出 Markdown 审计报告或 JSON 证据包，用于复盘、归档和后续自动评测。这个设计降低了大段 Markdown 输出带来的阅读成本，也提高了长链推理系统的可调试性、可信度和可复现性。
