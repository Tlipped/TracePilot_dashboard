{
    "name": "Cover Protocol",
    "cause": "Contract Vulnerability",
    "platform": "Ethereum",
    "time": "2020/12/28 8:08",
    "transaction_hash": [
        "0xd721b0ef2886f14b75548b70d2d1fd82bea085ca24f5de29b833a64cfd8f7a50",
        "0xadf27f5dd052482d46fdf69a5208a27cc7352522c7c19bbde5aee18f6ea4373b"
    ],
    "report": "The attacker exploited a critical state inconsistency between `memory` and `storage` in the `deposit` and `updatePool` functions. When `deposit` was called, it read the pool data into memory and then called `updatePool`, which correctly updated the key reward variable (`pool.accRewardsPerToken`) in storage. However, the subsequent `_claimCoverRewards` function incorrectly used the outdated value from memory (instead of the updated one in storage) to calculate mintable COVER tokens. Simultaneously, it also used an outdated `miner.rewardWriteoff` value because its update occurred after the minting calculation. This double accounting error allowed the attacker to mint vastly inflated amounts of COVER tokens with each deposit/withdraw cycle, leading to unlimited token minting and a profit of approximately $4.4 million for the initial attacker.",
    "detection": {
        "time": "2020/12/28 10:54",
        "link": "https://coverprotocol.medium.com/12-28-post-mortem-34c5f9f718d4"
    },
    "disclosure": {
        "time": "2020/12/28 23:51",
        "link": "https://twitter.com/emilianobonassi/status/1343839568919932933"
    },
    "root_cause": "deposit + updatePool",
    "report_link": "https://www.aicoin.com/zh-Hans/article/239546",
    "report_zh": "攻击者利用了 `deposit` 与 `updatePool` 函数中 `memory` 和 `storage` 之间的严重状态不一致。当调用 `deposit` 时，先将池数据读入内存，然后调用 `updatePool`，该函数在 storage 中正确更新了关键奖励变量（`pool.accRewardsPerToken`）。然而，随后的 `_claimCoverRewards` 函数错误地使用了内存中的过时值（而非 storage 中已更新的值）来计算可铸造的 COVER 代币。同时，它还使用了过时的 `miner.rewardWriteoff` 值，因为其更新发生在铸币计算之后。这个双重记账错误使得攻击者能够在每次存款/取款循环中铸造极度膨胀的 COVER 代币，导致无限制铸币，初始攻击者获利约 440 万美元。"
}
